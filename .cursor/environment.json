{
  "name": "Rushd",
  "install": "npm install && npx playwright install --with-deps chromium",
  "terminals": [
    {
      "name": "web",
      "command": "python3 -m http.server 8000",
      "description": "Serves the static Rushd app at http://localhost:8000 (open index.html)."
    }
  ],
  "ports": [
    {
      "name": "web server",
      "port": 8000
    }
  ]
}
